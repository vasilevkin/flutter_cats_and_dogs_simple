import 'package:flutter/material.dart';
import 'package:flutter_cats_and_dogs_simple/screens/home_screen.dart';

class AnimalApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(),
    );
  }
}
